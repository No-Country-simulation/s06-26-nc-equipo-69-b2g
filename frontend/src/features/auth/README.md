# Auth Feature

Owns the future frontend authentication experience.

This module is prepared as a boundary only. It does not define public/private routing yet. The landing page remains public, and private route grouping will be introduced later when authentication is implemented.

Planned internal structure:

```txt
components/
pages/
store/
```
